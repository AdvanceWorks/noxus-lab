line-length = 100
target-version = "py310"
extend-exclude = [".venv", "runs", "outputs", "scratch"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]  # line length handled by formatter

[format]
quote-style = "double"
indent-style = "space"
