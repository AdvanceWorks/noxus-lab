"""Workspace `__workspace__` — collection of related processes pushed to one Noxus workspace.

Each subfolder of this package is one process (one classifier + one
or more Noxus workflows + tests). Cross-process helpers, if any are
ever needed, belong upstream in `noxuslab`, not here.
"""
