# Knowledge sources for workspace `__workspace__`

Versioned source documents that back the Noxus knowledge bases used
by this workspace's agents and workflows. Drop policy PDFs,
spec documents, FAQ markdown, glossaries, etc. here. Each file should
be plain text or markdown when possible so diffs are readable.

Empty by default. Once you have at least one document, push the KB to
the platform with the relevant `noxuslab` command (TBD — for now,
upload via the Noxus UI and record the KB id in
[../README.md](../README.md)).

> Do not commit anything containing PII. Scrub before adding.
