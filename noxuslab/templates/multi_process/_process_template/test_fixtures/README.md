# Test fixtures for `__workspace__.__process__`

One `.txt` file per actionable label. **Filename without extension =
the expected label.** The test
`test_test_fixtures_cover_every_actionable_label` will fail the build
if any actionable label from `labels.py` has no fixture here.

Replace the placeholder files below with realistic examples taken from
production data (scrubbed of PII).
