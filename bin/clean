#!/bin/sh
# remove caches and venv
set -eu

rm -rf .venv .ruff_cache .pytest_cache .mypy_cache
find . -type d -name __pycache__ -prune -exec rm -rf {} +
find . -type f -name '*.py[co]' -delete
rm -rf runs outputs scratch
echo "clean."
